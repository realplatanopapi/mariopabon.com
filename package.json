{
  "private": true,
  "version": "1.3.1",
  "scripts": {
    "release:major": "npm version major -m '%s [deploy: production]'",
    "release:minor": "npm version minor -m '%s [deploy: production]'",
    "release:patch": "npm version patch -m '%s [deploy: production]'",
    "postversion": "git push && git push --tags"
  }
}
