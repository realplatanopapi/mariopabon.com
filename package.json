{
  "private": true,
  "scripts": {
    "build": "npm run jekyll && npm run styles",
    "develop": "concurrently 'jekyll serve' 'npm run styles -- --watch'",
    "jekyll": "jekyll build",
    "styles": "node-sass styles --output dist"
  },
  "devDependencies": {
    "concurrently": "3.4.0",
    "node-sass": "4.5.1"
  }
}
