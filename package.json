{
  "private": true,
  "scripts": {
    "build": "yarn clean && yarn static && parcel build index.html",
    "clean": "rm -rf dist/",
    "dev": "yarn clean && yarn static && parcel index.html",
    "static": "mkdir -p dist/ && cp -R static/* dist/"
  },
  "devDependencies": {
    "parcel": "^1.9.7"
  }
}
