{
  "private": true,
  "version": "0.10.0",
  "scripts": {
    "release:major": "npm version major -m '%s [deploy: production]' && git push --tags",
    "release:minor": "npm version minor -m '%s [deploy: production]' && git push --tags",
    "release:patch": "npm version patch -m '%s [deploy: production]' && git push --tags"
  }
}
