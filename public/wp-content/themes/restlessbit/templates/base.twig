{% block html_head_container %}

{% include 'html-header.twig' %}
	{% block head %}
	{% endblock %}
{% endblock %}

	<body>

    <nav class="global-nav">
      <a class="global-nav__brand bit{% if current_uri == '/' %} active{% endif %}"
				href="/" {% if current_uri == '/' %}aria-disabled="true"{% endif %}>
				<span class="sr-only">
					Home
				</span>
			</a>
			<a class="global-nav__link{% if current_uri == '/blog/' %} active{% endif %}"
				href="/blog/" {% if current_uri == '/blog/' %}aria-disabled="true"{% endif %}>blog</a>
    </nav>

		<main class="container">
			{% block content %}
				Sorry, no content
			{% endblock %}
		</main>

		{% block footer %}
			{% include 'footer.twig' %}
			{{ function('wp_footer') }}
		{% endblock %}

    <script src="{{ site.theme.link }}/assets/js/main.js"></script>
	</body>
</html>
